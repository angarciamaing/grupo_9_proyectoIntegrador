window.onload = () => {
    const name = document.getElementById('name');  // Coloca el  cursor en el pirmer campo del formulario al recargar la página
    name.focus();

    // const form = document.getElementById('editProductForm');
    // form.onsubmit = (e) => {
    //     e.preventDefault();
    //     console.log('hola desde sunmit');

    // }
}