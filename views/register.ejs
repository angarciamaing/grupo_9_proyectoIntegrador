<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro</title>
    <link rel="icon" type="image/x-icon" href="/img/logoFace.svg">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" /> <!--Libreria fontawesome-->
    <link rel="stylesheet" href="/css/register.css"> <!--Enlazar HTML y CSS-->
    <script src="https://code.iconify.design/2/2.1.2/iconify.min.js"></script>
</head>
<body>    
      <!-- HEADER-->
  <%- include('partials/header') %>

    <main class="register">
        
        <form method="POST" action="/user/register" enctype="multipart/form-data">
            <h1>Crear cuenta</h1>
            <label for="fullname"><span class="iconify" data-icon="carbon:user-avatar-filled-alt" data-width="25" data-height="25"></span>
                <input type="text" placeholder="Nombre completo"  name="fullname" id="fullname" value="<%=locals.old && old.fullname ? old.fullname :'' %>">
                <% if (locals.errors && errors.fullname) { %>
                    <div class="text-danger">
                        <%= errors.fullname.msg %> 
                    </div>
                <% } %>
            </label>
            <label for="userName"><span class="iconify" data-icon="carbon:user-avatar-filled-alt" data-width="25" data-height="25"></span>
                <input type="text" placeholder="Nombre de Usuario"  name="username" id="userName" value="<%=locals.old && old.username ? old.username:'' %>">
                <% if (locals.errors && errors.username) { %>
                    <div class="text-danger">
                        <%= errors.username.msg %> 
                    </div>
                <% } %>
            </label>
            <label for="email"><span class="iconify" data-icon="cib:mail-ru" data-width="20" data-height="20"></span>
                <input type="text" placeholder="Email"  name="email" id="userName" value="<%= locals.old && old.email ? old.email : '' %> ">
                <% if (locals.errors && errors.email) { %>
                    <div class="text-danger">
                        <%= errors.email.msg %> 
                    </div>
                <% } %>
            </label>  
            <label for="profilePicture"><span class="iconify" data-icon="jam:picture" data-width="25" data-height="25"></span>
                <div  class="file-upload"> 
                    <p>Sube tu imagen de perfil</p>
                    <input  type="file" name="profilePicture" id="profilePicture" >                    
                </div>
                <% if (locals.errors && errors.profilePicture) { %>
                    <div class="text-danger">
                        <%= errors.profilePicture.msg %> 
                    </div>
                <% } %>
            </label>
            <label for="password"><span class="iconify" data-icon="bxs:lock" data-width="20" data-height="20"></span>
                <input type="password" placeholder="Constraseña"  name="password" id="password">
                <% if (locals.errors && errors.password) { %>
                    <div class="text-danger">
                        <%= errors.password.msg %> 
                    </div>
                <% } %>
            </label>
            <label for="re_password"><span class="iconify" data-icon="bxs:lock" data-width="20" data-height="20"></span>
                <input type="password" placeholder="Repite tu contraseña"  name="re_password">
                <% if (locals.errors && errors.re_password) { %>
                    <div class="text-danger">
                        <%= errors.re_password.msg %> 
                    </div>
                <% } %>
            </label>       
            
            <div class="buttons">
                <input type="submit" value="Registrarse" class="btn">
            </div>
        </form>
    </main>
    
    <!-- FOOTER-->
   <%- include('partials/footer') %>
    
</body>
</html>