<!DOCTYPE html>
<html lang="es">
	<head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link rel="icon" type="image/x-icon" href="/img/logoFace.svg">
        <!-- Bootstrap Grid Stylesheet -->
        <!--<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous"> --> 
	
        <link rel="stylesheet" href="/css/products-create-form.css">
        <script src="https://kit.fontawesome.com/6fda1860f4.js" crossorigin="anonymous"></script>

		<script src="https://code.iconify.design/2/2.1.2/iconify.min.js"></script>

        <title>Edición de producto</title>
    </head>
<body>
	<!-- Header -->
	<%- include('partials/header') %>
	<!-- /Header -->
	<main class="create">
		<form action="/products/edit/<%= product.id %>" method="POST" enctype="multipart/form-data">
            <div class="image-container">
				<img src="/img/logoFace.png" alt="">
			</div>
			<h1 class="products-title">Editar producto</h1>			
			<label for="name">
				<input type="text" id="name" name="name" placeholder="Nombre del producto" class="form-input" value="<%= product.product_name %>">
			</label>
			<label for="description">
                    <input type="text" id="description" name="description" placeholder="Descipción del producto" class="form-input" value="<%= product.product_description %>">
			</label>
			
			<label for="image">
				<div  class="file-upload"> 
					<p>Imagen del producto</p>
					<input  type="file" name="image" id="image" accept=".jpg" value="<%= product.product_image %>" > 
				</div>					
			</label>
			<label for="category"></label>
			<select name="category" id="category" class="form-input">
                <% for(let i = 0; i < category.length; i ++){ %>
                    <% if(category[i].id == product.category_id) { %>
                        <option value="<%=category[i].id %>" selected>
                            <%=category[i].name_category %>
                        </option>
                        <% } else { %>
                            <option value="<%=category[i].id %>">
                                <%=category[i].name_category %>
                            </option>
                            <% } %>
                    
               <% } %>
                	
		
			<label for="price">
				<input type="text" id="price" name="price" placeholder="Precio del producto" class="form-input" value="<%= product.price %>">
			</label>					
			<div class="buttons">
                <input type="submit" value="Editar producto" class="btn">
            </div>





			
		</form>
	</main>
		

	<!-- footer -->
	<%- include('partials/footer') %>
	<!-- /footer -->
</body>	
</html>